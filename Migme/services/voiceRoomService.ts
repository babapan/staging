/**
 * voiceRoomService.ts
 *
 * Classic chatroom voice room — sudah tidak aktif (voice room dihapus dari
 * classic chatroom). File ini dipertahankan untuk backward compatibility
 * tapi tidak ada dependency Agora di sini.
 *
 * Untuk Live Party, lihat partyService.ts
 */

export {};
